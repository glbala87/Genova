apiVersion: v1
kind: Service
metadata:
  name: {{ include "genova.fullname" . }}
  labels:
    {{- include "genova.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "genova.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
